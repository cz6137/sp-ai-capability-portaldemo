package com.spai.portal.task.repository;import com.spai.portal.task.domain.TaskProgress;import java.util.*;import org.springframework.data.jpa.repository.JpaRepository;public interface TaskProgressRepository extends JpaRepository<TaskProgress,String>{Optional<TaskProgress>findByTaskIdAndTeamId(String taskId,String teamId);List<TaskProgress>findByTaskIdIn(Collection<String>taskIds);}
